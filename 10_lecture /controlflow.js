// if 
const isUserloggedIn = true

if (2=="2"){
    console.log("executed")

} else{
    console.log("not equal")
}

const score = 200
if(score > 100) {
    const power = "fly"
    // var power = "fly" it will 
    console.log(`user power: ${power}`)
}
// console.log(`user power: ${power}`) // not accessible bcz value cant acces from local scope to global scope.
 // if at the place of const VAR is used than the value can be easily accessed from local scope to  global scope 
//  console.log(`user power: ${power}`) // error

const balance = 1000
// if (balance>500) console.log("test"), condole.log("test2"); 
// it will give the output bbut its not the right way to write the code 

// else if


const userLoggedIn = true
const debitCard = true
const loggedInFromGoogle = false
const loggedInFromEmail = true

if(userLoggedIn && debitCard $$ 2===3){
    console.log("allow to buy course")
}
if (loggedInFromGoogle || loggedInFromEmail){
    console.log("user logged in ")
}
